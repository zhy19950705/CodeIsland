import Foundation

// Centralize UI display locale so relative dates and formatted values stay Chinese on every system language.
enum AppLocale {
    static let chinese = Locale(identifier: "zh_Hans_CN")
}
